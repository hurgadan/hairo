/**
 * Витринная группировка каталога (чипы над гридом) — не ось таксономии из
 * `CATALOG.md`, а способ разложить 30 карточек по понятным полкам.
 * Подписи живут в словарях фронта: язык интерфейса меняется, код — нет.
 */
export enum HairstyleGroup {
  Bob = "bob",
  Curly = "curly",
  MidLength = "mid_length",
  Long = "long",
  Short = "short",
  Masculine = "masculine",
  /** Причёски «на выход»: укладки и собранные варианты для события. */
  Event = "event",
}
